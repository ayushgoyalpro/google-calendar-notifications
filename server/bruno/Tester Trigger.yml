info:
  name: Tester Trigger
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/test/trigger?key={{your_key}}"
  params:
    - name: key
      value: "{{your_key}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
